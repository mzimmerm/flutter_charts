enum ExternalTickAtPosition {
  childStart,
  childCenter,
  childEnd,
}

enum LayoutAxis {
  horizontal,
  vertical,
}

// todo-00-refactoring : Move to morphic/chart_support
enum DataDependency {
  inputData,
  outputData,
}
