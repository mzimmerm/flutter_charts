enum ExternalTickAtPosition {
  childStart,
  childCenter,
  childEnd,
}

enum LayoutAxis {
  horizontal,
  vertical,
}

// todo-001-refactoring : Move to morphic/chart_support
enum DataDependency {
  inputData,
  outputData,
}
