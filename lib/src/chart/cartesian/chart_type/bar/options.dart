import 'package:flutter/foundation.dart' show immutable;

/// The options specific for the [BarChart].
@immutable
class BarChartOptions {
  /// Constructor of [BarChartOptions]. No options specific for vertical bar chart.
  const BarChartOptions();
}
